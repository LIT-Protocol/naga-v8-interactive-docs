# Features
- [ ] The LitAuthProvider should accept a config file that allow user to enable/disable certain auth methods
- [ ] If user select WebAuthn, then "Mint New PKP" button should be disabled if the user has already minted a PKP, because one passkey is tied to only one PKP.
- [ ] The LitAuthProvider should accept a auth service url, and should allow user to hide the fields.
- [ ] LitAuthProvider should allow to pass in the Lit Network to use.
- [ ] Allow user to add custom chains.